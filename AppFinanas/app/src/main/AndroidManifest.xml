<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ufop.guilherme.appfinanas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistroActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DespesasActivity" />
        <activity android:name=".ExibeDespActivity" />
        <activity android:name=".RendaActivity" />
        <activity android:name=".ExibeRendaActivity" />
        <activity android:name=".EditDespActivity" />
        <activity android:name=".EditRendActivity" />
        <activity android:name=".AfterLoginActivity" />
    </application>

</manifest>